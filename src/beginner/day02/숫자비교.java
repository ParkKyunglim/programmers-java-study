package beginner.day02;

public class 숫자비교 {
    // 두 수가 같으면 1 다르면 -1을 retrun
    public int solution(int num1, int num2) {
//        int answer = (num1 == num2) ? 1 : -1; // 변수 선언
        return num1 == num2 ? 1 : -1; // 간결하게 바로 return
    }
}
